# Code Blocks

Experience premium code visualization with syntax highlighting and filename labels.

## TypeScript Example

```typescript filename="example.ts"
function greet(name: string) {
  console.log(`Hello, ${name}!`);
}

greet("Nextra");
```

## CSS Example

```css filename="styles.css"
.premium-card {
  background: white;
  border-radius: 1rem;
  box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1);
}

.dark .premium-card {
  background: #111;
  border: 1px solid #222;
}
```
